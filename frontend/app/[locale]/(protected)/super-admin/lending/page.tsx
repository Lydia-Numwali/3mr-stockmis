import React from 'react';
import LendingContainer from '@/components/containers/lending/lending-container';

const LendingPage = () => {
    return (
        <div>
            <LendingContainer />
        </div>
    );
};

export default LendingPage;
